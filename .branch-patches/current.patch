diff --git a/app/src-tauri/src/harness/sidecar.rs b/app/src-tauri/src/harness/sidecar.rs
index a02b2b6..0000000 100644
--- a/app/src-tauri/src/harness/sidecar.rs
+++ b/app/src-tauri/src/harness/sidecar.rs
@@ -664,7 +664,8 @@ pub fn validate_root_snapshot(snapshot: &RootSessionSnapshot) -> bool {
             .detail
             .as_ref()
             .is_none_or(|value| valid_label(value))
         && match snapshot.worker_recovery.status {
-            WorkerRecoveryStatus::Starting => {
+            WorkerRecoveryStatus::Starting
+            | WorkerRecoveryStatus::Stopping => {
                 snapshot.worker_recovery.closure_reason.is_none()
                     && snapshot.worker_recovery.observation_id.is_none()
                     && snapshot.worker_recovery.automatic_retry_count == 0
