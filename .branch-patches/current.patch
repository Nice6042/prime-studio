diff --git a/.branch-patches/wave-2-marker b/.branch-patches/wave-2-marker
new file mode 100644
index 0000000..d4e6c01
--- /dev/null
+++ b/.branch-patches/wave-2-marker
@@ -0,0 +1 @@
+provider-account-usage-truth
diff --git a/.branch-patches/commands.sh b/.branch-patches/commands.sh
index dc88f7c..0000000 100755
--- a/.branch-patches/commands.sh
+++ b/.branch-patches/commands.sh
@@ -150,7 +150,7 @@ accounts_test = replace_once(
     expect(await screen.findByRole("status")).toHaveTextContent(reason);
     expect(screen.getByRole("button", { name: "Use for new sessions" })).toBeDisabled();
     expect(screen.getByRole("button", { name: "Set as default" })).toBeDisabled();
-    expect(screen.getByRole("button", { name: /log in|re-login/i })).toBeEnabled();
+    expect(screen.getByRole("button", { name: "Re-login" })).toBeEnabled();
     expect(screen.getByRole("button", { name: "Rename" })).toBeEnabled();
     expect(screen.getByRole("button", { name: "Remove" })).toBeEnabled();
   });
